<p>
  <b>Venue:</b>
  <%=h @meeting.venue_id %>
</p>

<p>
  <b>Scheduled at:</b>
  <%=h @meeting.scheduled_at %>
</p>

<p>
  <b>Details:</b>
  <%= render_textile(@meeting.details) %>
</p>

<% if logged_in? && current_user.admin? %>
<%= link_to 'Edit', edit_meeting_path(@meeting) %> |
<% end %>
<%= link_to 'Back', meetings_path %>
